#' Probability core function for binomial RDT
#'
#' Define the inside sum core of integration function which is the probability of passing the test (Only for binomial RDT)
#'
#' @param n RDT sample size.
#' @param c Maximum allowable failures.
#' @param pi Failure probability.
#' @return Summed probability
#' @examples
#' bcore(n = 10, c = 2, pi = 0.2)
#' 0.6777995


bcore <- function(n, c, pi){
  return(sum(dbinom(c(0:c), n, pi)))
}
